import Placeholder from "@/pages/Placeholder";

export default function Saved() {
  return (
    <Placeholder
      title="Saved"
      description="Bookmarks for posts, events, members, and opportunities (MVP next)."
    />
  );
}

